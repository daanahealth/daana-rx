'use client';

// Provider home is owned by the provider feature (specialty-first search +
// request flow); this re-export keeps HomeScreen's role branch stable.
export { ProviderHomeScreen as ProviderHome } from '@/features/provider/ProviderHomeScreen';
